#pragma once

#include <iostream>
#include <fstream>
#include <tuple>
#include <sstream>
#include <string>

std::string get_file(int argc, char* argv[]);
std::string get_file_as_string(std::string filename);